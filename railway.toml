[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && java $JAVA_OPTS -jar target/*.jar"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
